# 实现原理
跳表本质上就是一个多层链表，上层链表是下层的子集，查找元素从上层链表往下查找，从左往右查找，如何查找的元素在这一层是最大，就会跳到下一层去查找元素，直到找到元素为止，最底层链表存储所有的元素，如果在底层外所有的上层链表都没有找到元素，那就只可以在最底层找到元素了
# 插入数据的原理
先按照查找的方式找到插入的地方，然后随机决定新的节点是要建立在哪几层索引上的
# 删除数据的原理
先找到要删除的节点，然后把这个节点在各个层的索引都进行删除