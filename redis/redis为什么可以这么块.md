# 内存存储
cup访问内存走的是总线，数据直接从内存条的DRAM芯片读到CPU缓存中的，整个过程都是电信号的
# 单线程+IO多路复用
1. redis用单线程执行命令，，这个过程没有锁的竞争，没有上下文的切换
2. 网路用epoll多路复用，一个线程负责多个连接，，哪个连接整备好，就处理哪个连接
# 高效的数据结构
1. String用的是SDS
2. Hash小数据用ziplist ，大数据用Hashtable
3. Zset用跳表