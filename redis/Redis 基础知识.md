# 常见的数据类型
* String：这个是最常见的数据类型，能够存储数字，字符串，二进制数，最大的存储内存是512MB
* List：一个有序的字符串列表，底层是双向链表，支持两端的操作
* Set：无序不可以重复的集合
* Hash：一个建值对集合
* Zset：带有一个score分数进行排序的set
* BitMap：用位来存储数据的，每一个bit只占0或者1，空间的使用程度很高
* Stream：5.0版本引入的，专门为消息队列设计的，两个关键的特点，其一会自动生成全局唯一的信息ID，其二可以信息持久化
* GEO：