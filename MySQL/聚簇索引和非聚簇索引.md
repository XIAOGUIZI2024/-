# 聚簇索引
聚簇索引它的叶子节点存储的是完成的数据，一张表中通常就只用一个聚簇索引，一般为表的主键，如果表没有主键的话会自动生成一个隐藏的主键的
# 非聚簇索引
这个非聚簇索引构建的b+树叶子节点存储的主键和引列的值，查询得数据不是索引上的值，在叶子节点获取主键后再去聚簇索引构建的b+树获取完成的数据，这个操作就是回表

# b+树
* 叶子节点才存储数据
* 非叶子节点存储叶子节点的主键和页号
* 叶子节点之间有xuan